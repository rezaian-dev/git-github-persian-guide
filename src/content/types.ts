// انواع مشترک لایهٔ محتوای کتاب
// Shared types for the book content layer.

export type ReaderChapter = {
  n: number;
  num: string;
  title: string;
  label: string;
  part: number;
  html: string;
};
